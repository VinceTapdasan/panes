PORT=3001

# CORS (comma-separated origins, or * for all)
CORS_ORIGINS=http://localhost:3000

# Firebase Admin SDK
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
FIREBASE_STORAGE_BUCKET=

# Pane settings
PANE_TTL_HOURS=72
PANE_MAX_SIZE_MB=5

# Rate limiting (per IP)
THROTTLE_TTL=60000
THROTTLE_LIMIT=60

# Cleanup job (optional - if not set, endpoint is unprotected)
CLEANUP_API_KEY=
